[build]
  command = "npm run build"
  publish = "/public"

[[headers]]

  [headers.values]
   
    # Multi-value headers are expressed with multi-line strings.
    cache-control = '''
    max-age=0,
    no-cache,
    no-store,
    must-revalidate'''