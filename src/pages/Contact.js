import React from 'react'
import { Layout, Form } from '../components'

const ContactUs = () => {
    return (
        <Layout>
            <Form />
        </Layout>
    )
}

export default ContactUs
